{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "Run target",
          "type": "cppdbg",
          "request": "launch",
          "preLaunchTask": "CMake: build",
          "program": "${command:cmake.launchTargetPath}",
          "cwd": "${workspaceFolder}/build"
       }
  ]
}
